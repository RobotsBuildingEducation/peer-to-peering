import React from "react";

/**
 * A simple component that renders a static message indicating it is empty.
 * This component can be used as a placeholder or for demonstration purposes.
 */
export const EmptyComponent = () => {
  return <div>An empty component</div>;
};
